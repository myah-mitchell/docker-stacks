# Host setup for step-ca. scripts/build.py renders it into each stack README, and
# the ansible stacks role applies it. See scripts/project-layout.md.
folders:
  - path: step-ca-data
    owner: 101000
    group: 101000
  - path: step-ca-secrets
    owner: 101000
    group: 101000
    mode: "0700"
