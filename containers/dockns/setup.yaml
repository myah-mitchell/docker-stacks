# Host setup for dockns. scripts/build.py renders it into each stack README, and
# the ansible stacks role applies it. See scripts/project-layout.md.
folders:
  - path: dockns-data
    owner: 100000
    group: 100000
