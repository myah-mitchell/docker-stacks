###############################################################################
# Start of stack config
###############################################################################
# Project Name: "step-ca"
name: ${PROJECT_NAME}

networks:
  proxy:
    name: ${PROXY_NETWORK}
    external: true
#  frontend:
#    name: ${PROJECT_NAME}_frontend
#    internal: false
#  backend:
#    name: ${PROJECT_NAME}_backend
#    internal: true
#  socket_proxy:
#    name: ${PROJECT_NAME}_socket_proxy
#    internal: true

services:

  step-ca:
    extends:
      file: ../../containers/step-ca/compose.yaml
      service: .step-ca
