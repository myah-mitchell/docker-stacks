# Host setup for uptime-kuma. scripts/build.py renders it into each stack README, and
# the ansible stacks role applies it. See scripts/project-layout.md.
folders:
  - path: uptime-kuma-data
    owner: 101000
    group: 101000
