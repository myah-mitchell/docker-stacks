# Host setup for postgres. scripts/build.py renders it into each stack README, and
# the ansible stacks role applies it. See scripts/project-layout.md.
folders:
  - path: postgres-data
    owner: 100000
    group: 100000
