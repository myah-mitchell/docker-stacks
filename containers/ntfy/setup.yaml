# Host setup for ntfy. scripts/build.py renders it into each stack README, and
# the ansible stacks role applies it. See scripts/project-layout.md.
needs_host:
  - name: traefik
folders:
  - path: ntfy-data
    owner: 101000
    group: 101000
