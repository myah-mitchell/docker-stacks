# Host setup for crowdsec. scripts/build.py renders it into each stack README, and
# the ansible stacks role applies it. See scripts/project-layout.md.
needs_stack:
  - name: socket-proxy
folders:
  - path: crowdsec-data
    owner: 100000
    group: 100000
  - path: crowdsec-config
    owner: 100000
    group: 100000
