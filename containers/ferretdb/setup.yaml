# Host setup for ferretdb. scripts/build.py renders it into each stack README, and
# the ansible stacks role applies it. See scripts/project-layout.md.
folders:
  - path: ferretdb-data
    owner: 101000
    group: 101000
    services: [ferretdb]
  - path: postgres-data
    owner: 100000
    group: 100000
    services: [postgres]
