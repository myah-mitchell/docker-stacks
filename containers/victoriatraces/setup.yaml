# Host setup for victoriatraces. scripts/build.py renders it into each stack README, and
# the ansible stacks role applies it. See scripts/project-layout.md.
folders:
  - path: victoriatraces-data
    owner: 101000
    group: 101000
