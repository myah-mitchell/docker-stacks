# Host setup for semaphore. scripts/build.py renders it into each stack README, and
# the ansible stacks role applies it. See scripts/project-layout.md.
folders:
  - path: semaphore-data
    owner: 101001
    group: 101001
  - path: semaphore-config
    owner: 101001
    group: 101001
  - path: semaphore-tmp
    owner: 101001
    group: 101001
